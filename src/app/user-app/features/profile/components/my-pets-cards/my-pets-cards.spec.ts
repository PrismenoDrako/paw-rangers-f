/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MyPetsCards } from './my-pets-cards';

describe('MyPetsCards', () => {
  let component: MyPetsCards;
  let fixture: ComponentFixture<MyPetsCards>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [MyPetsCards]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MyPetsCards);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/