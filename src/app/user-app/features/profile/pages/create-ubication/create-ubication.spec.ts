import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateUbicationPage } from './create-ubication';
/*
describe('CreateUbication', () => {
  let component: CreateUbicationPage;
  let fixture: ComponentFixture<CreateUbicationPage>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [CreateUbicationPage]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CreateUbicationPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/